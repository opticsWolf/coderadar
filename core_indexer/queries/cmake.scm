; CodeRadar v3.6 — CMake tree-sitter queries
; tree-sitter-cmake via tree-sitter-language-pack
; CMake grammar uses generic nodes — best served by regex layer.

;; ── Bracketed arguments (multi-line strings) ────────────────────────

(bracket_argument) @docstring
